#include <stdio.h>
#include <stdlib.h>

int main()
{
    int i,j,s[100];
    char text[200];
    printf("Veuillez saisir un texte\n");
    fflush(stdin);
    gets(text);
    puts(text);

    return 0;
}
